# Test Oracle Design Result Folder

Expected artifact:

- `test_oracle_design.md`

This artifact should contain the test strategy, source-contract tests, fixture candidates, and an optional plan for generating exact `skimage.morphology.medial_axis` golden outputs later.
